const StudentsData = require('./students.js');

async function SendCoupons(whatsAppNumber){
    const student = students[id];
    if (student) {
        return student;
    } 
}


module.exports = SendCoupons;